enum Edge: CaseIterable {
    /// `-`
    case horizontal

    /// `|`
    case vertical

    /// `」`
    case topAndLeft

    /// `└`
    case topAndRight

    /// `┐`
    case bottomAndLeft

    /// `「`
    case bottomAndRight
}
